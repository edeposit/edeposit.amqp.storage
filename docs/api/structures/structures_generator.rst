structures_generator script
===========================

This script is used to generate :class:`.Publication`, :class:`.DBPublication`, :class:`.Archive` and :class:`.DBArchive` structures.

.. automodule:: storage.structures.structures_generator
    :members:
    :undoc-members:
